{
  "name": "chronoverse",
  "version": "1.0.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/Yale-Giles/Chronoverse.git"
  },
  "description": "Decentralized time-capsule and inheritance protocol",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "test:coverage": "hardhat coverage",
    "deploy": "hardhat run scripts/deploy.js",
    "deploy:arbitrum": "hardhat run scripts/deploy.js --network arbitrum",
    "verify": "hardhat run scripts/verify.js",
    "node": "hardhat node",
    "clean": "hardhat clean",
    "lint": "solhint 'contracts/**/*.sol'",
    "format": "prettier --write 'contracts/**/*.sol' 'test/**/*.js'"
  },
  "keywords": [
    "blockchain",
    "inheritance",
    "timelock",
    "ethereum",
    "solidity"
  ],
  "author": "Chronoverse Team",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "hardhat": "^2.19.0",
    "chai": "^4.3.10",
    "ethers": "^6.9.0"
  },
  "dependencies": {
    "@chainlink/contracts": "^0.8.0"
  }
}

